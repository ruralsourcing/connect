{
  "name": "caspr-responder",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:dfederspiel/caspr-responder.git",
  "author": "David Federspiel <david@federnet.com>",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {
    "@material-ui/icons": "^4.11.2",
    "@prisma/cli": "^2.14.0",
    "@prisma/client": "^2.14.0"
  },
  "scripts": {
    "setup": "yarn install && (cd www && yarn install) && (cd server && yarn install)"
  }
}
